diff -Naur kodi-15.0-beta1-0c4789d/tools/depends/native/TexturePacker/src/configure.ac kodi-15.0-beta1-0c4789d.patch/tools/depends/native/TexturePacker/src/configure.ac
--- kodi-15.0-beta1-0c4789d/tools/depends/native/TexturePacker/src/configure.ac	2015-04-02 09:58:19.000000000 +0200
+++ kodi-15.0-beta1-0c4789d.patch/tools/depends/native/TexturePacker/src/configure.ac	2015-04-02 12:44:42.000000000 +0200
@@ -14,9 +14,11 @@
 AC_ARG_ENABLE([static],
   [AS_HELP_STRING([--enable-static],
   [build static TexturePacker (default is no)])],
-  [STATIC_FLAG="-static"],
-  [STATIC_FLAG=""])
-
+  [if test x$enableval = xyes; then
+     STATIC_FLAG="-static"
+   else
+     STATIC_FLAG=""
+  fi])
 
 PKG_CHECK_MODULES([PNG], [libpng],
   [INCLUDES="$PNG_CFLAGS"; LIBS="$LIBS $(pkg-config --silence-errors --static --libs libpng)"],
